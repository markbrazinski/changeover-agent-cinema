# Scripts Directory

Development measurement, qualification harnesses, and service launchers.

- **`run_server.py`**: Main FastAPI backend launcher (`http://localhost:8000`).
- **`fetch_media.sh`**: Helper script to ensure `public/media/` assets are populated.
- **`measure_*.py` / `run_*.sh`**: Specialized development measurement and qualification harnesses used during development and testing.
